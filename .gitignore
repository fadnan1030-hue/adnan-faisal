# Excel lock files
~$*

# Auto-generated backups (churn, not source)
Backups/

# Screen recordings (large, not part of the deliverable)
*.mp4

# Firebase CLI local cache (deploy metadata, not source)
.firebase/
